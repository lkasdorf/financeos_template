# Claude Code Memory

This directory holds the persistent memory files Claude Code maintains about
your project. Files here are written by the assistant during normal use and
synchronised into the repo via the `MEMORY SYNC` command (see CLAUDE.md).

The directory ships empty. There is nothing to edit by hand.
